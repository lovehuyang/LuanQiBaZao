//: Playground - noun: a place where people can play
// oc中有隐式转换，可以把两个类型不同的数值相加
//swift中没有隐式转换，数值进行运算时需要把类型转换成相同的
let a = 10 // a为整型
let b = 2.9 // b 为浮点型
let temp = Double(a)// 类型转化： 把整型a 转换成 浮点型temp
print(a)
let c = temp + b // 同类型才能运算













